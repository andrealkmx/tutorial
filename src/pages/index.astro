---
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "Spark";
---
<BaseLayout pageTitle={pageTitle}>
  <div class="relative w-full min-h-screen">
    <div class="absolute inset-0">
      <img 
        src="/images/hero-image.png" 
        alt="Hero image" 
        class="w-full h-full object-cover"
      />
    </div>
    <div class="relative z-10 flex items-center justify-center min-h-screen w-full">
      <h2 class="text-white text-center text-4xl font-bold w-full max-w-6xl mx-auto px-4">We innovate human-centric strategies for your adoption through OCM.</h2>
    </div>
  </div>
</BaseLayout>